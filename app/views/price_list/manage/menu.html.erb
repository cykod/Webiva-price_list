<% admin_form_for @price_list, :as => :menu do |f| -%>
  <%= f.text_field :name, :required => true %>
  <%= f.filemanager_image :image_id %>
  <%= f.submit @price_list.id ? 'Update menu'.t : 'Create menu'.t %>
<% end -%>
